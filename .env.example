# URL pública do seu projeto Supabase (encontra no dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://fybgmjofigsazvyiphot.supabase.co

# Chave service_role do Supabase (NUNCA exponha no client-side!)
# Dashboard → Settings → API → service_role key
SUPABASE_SERVICE_ROLE_KEY=

# Segredo para assinar tokens JWT (gere com: openssl rand -base64 32)
JWT_SECRET=
